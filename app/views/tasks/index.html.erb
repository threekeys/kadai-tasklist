<h1>タスク一覧</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= link_to task.id, task %> : <%= task.status %> &gt;　<%= task.content %></li>
  <% end %>
</ul>
<%== pagy_nav(@pagy) %>

<%= link_to '新しいタスクの登録', new_task_path %>